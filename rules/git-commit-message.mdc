---
description: 当用户请求生成 Git commit 消息、提交代码、或在 Source Control 面板生成描述时触发。确保提交信息符合中文规范和 Conventional Commits 格式。
alwaysApply: false
---
# Git 提交规范 (继承全局原则)
- **语言要求**：必须使用 **中文 (简体)**。
- **核心格式**：遵循 Conventional Commits 规范，格式为: `<类型>(<范围>): <描述>`。
- **常见类型**：
  - `feat`: 新功能
  - `fix`: 修补 bug
  - `docs`: 文档修改
  - `style`: 格式说明 (不影响代码运行的变动)
  - `refactor`: 重构 (即不是新增功能，也不是修改 bug 的代码变动)
  - `test`: 增加测试
  - `chore`: 构建过程或辅助工具的变动
- **结构**：
   - 第一行：Emoji + 类型 + 简短摘要（不超过 50 个字符）。
   - 第二行：留空。
   - 第三行及以后：详细列出主要的改动点，使用 "-" 符号。
- **具体要求**：
  - **事实为本**：基于当前的 Git Diff 真实变动生成，严禁编造未包含的功能。
  - **关联性**：如果是 Java 项目，范围 `(<范围>)` 优先使用模块名或核心类名。
  - **禁止**：严禁输出类似 "Here is the commit message" 这种开场白，直接输出消息内容。
、
# 示例
feat(auth): 增加用户登录敏感词过滤
fix(order): 修复订单金额计算精度问题